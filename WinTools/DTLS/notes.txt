1. openssl req -newkey rsa:4096 -nodes -x509 -keyout key.pem -out cert.crt

== OR ==
1. openssl genrsa -out key.pem 16384
2. openssl req -new -x509 -key key.pem -out cert.crt --days 360

== THEN ==

openssl s_server -CAfile bundle.pem -cert ./clientB.crt -key ./clientB.key -accept 8443 -cipher "TLS_RSA_WITH_AES_128_GCM_SHA256" -dtls1_2 -listen

echo "Hello World" | openssl s_client -dtls1_2 -connect 192.168.1.246:8443